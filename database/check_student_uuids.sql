-- List all user_id values in the students table
SELECT user_id FROM students;

-- List all user_id values in the user_activity_progress table
SELECT user_id FROM user_activity_progress;

-- List all user_id values in the auth.users table (Supabase Auth)
SELECT id FROM auth.users;